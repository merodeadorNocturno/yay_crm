<nav class="level">
  <!-- Left side -->
  <div class="level-left">
    <div class="level-item"></div>
  </div>

  <!-- Right side -->
  <div class="level-right control has-icons-left">
    <div class="level-item">
      <a
        class="button is-success"
        hx-post="{{conf.server_protocol}}://{{conf.server_address}}:{{conf.server_port}}/new_user"
        hx-swap="innerHTML"
        hx-target="#edit-user-dynamic-content"
      >
        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;Usuario</a
      >
      <span class="icon is-small is-left">
        <i class="fas fa-plus-circle"></i>
      </span>
    </div>
    <p class="level-item"><a></a></p>
    <p class="level-item"></p>
  </div>
</nav>
<div class="columns">
  <div class="column" id="user-table-dynamic-content">
    <table class="table is-bordered is-striped is-hoverable">
      <thead>
        <tr>
          <th>Name</th>
          <th>Email</th>
          <th>Role</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        {{#each users}}
        <tr>
          <td>{{name}} {{last_name}}</td>
          <td>{{email}}</td>
          <td>{{role}}</td>
          <td>
            <span
              hx-post="{{../conf.server_protocol}}://{{../conf.server_address}}:{{../conf.server_port}}/user_htmx/{{uuid}}"
              hx-swap="innerHTML"
              hx-target="#edit-user-dynamic-content"
              class="icon is-small is-left"
            >
              <i class="fas fa-pencil-alt" style="cursor: pointer"></i>
            </span>
            &nbsp;
            <span
              class="icon is-small is-left"
              hx-on:click="press_trash({{uuid}})"
            >
              <i class="fas fa-trash" style="cursor: pointer"></i>
            </span>
          </td>
        </tr>
        {{/each}}
        <!-- Add more rows for other users -->
      </tbody>
    </table>
  </div>
  <div class="column" id="edit-user-dynamic-content"></div>
</div>
